API_PREFIX=/api/v1
SERVICE_PORT=8080

DB_USER=postgres
DB_PASSWORD=8931gugu567
POSTGRES_PASSWORD=8931gugu567
DB_NAME=awebo
DB_HOST=db
DB_PORT=5432

JWT_SECRET=8931gugu567-dev-secret-change-me
JWT_TTL_HOURS=720

CODE_TTL_SECONDS=600
CODE_RESEND_SECONDS=30

# console just logs the code to stdout — good for local dev.
# switch to smtp and fill in the SMTP_* vars to actually send email.
MAIL_DRIVER=console
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=no-reply@awebo.example

UPLOADS_DIR=uploads
MAX_AVATAR_SIZE_MB=5
